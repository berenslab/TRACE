# contrastive_learning_neuro
This repository implements contrastive learning for timeseries data particularly for neuroscience data. The data needs to have multiple trials because single trials (or the means over several trials) are used to generate positive pairs.
