# Contrastive_learning_neuro
TRACE: Contrastive learning for timeseries (neuro) data

Maintainer of the repository: Lisa Schmors


For the TS2Vec model, install the following fork:

```bash
pip install git+https://github.com/sdamrich/ts2vec.git
```
